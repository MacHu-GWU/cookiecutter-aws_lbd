# -*- coding: utf-8 -*-

"""
Root package for the ``{{ cookiecutter.package_name }}`` project.

This package contains all application code that is deployed to AWS Lambda,
as well as local-only tooling for building, testing, and deploying.  The
public API surface is re-exported through ``api.py`` — external code should
import from ``{{ cookiecutter.package_name }}.api`` rather than reaching into
internal submodules.
"""
